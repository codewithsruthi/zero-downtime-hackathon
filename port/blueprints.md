# Port blueprints

JSON files in `port/blueprints/` match the four identifiers in `port/brief.md`.

Import them in Port (or treat them as the contract if the org login is dark). The factory upserts entities with those identifiers; it does not edit blueprints at runtime.
